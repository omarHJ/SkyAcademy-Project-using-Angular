import { VacationDaysLeftPipe } from './vacation-days-left.pipe';

describe('VacationDaysLeftPipe', () => {
  it('create an instance', () => {
    const pipe = new VacationDaysLeftPipe();
    expect(pipe).toBeTruthy();
  });
});
